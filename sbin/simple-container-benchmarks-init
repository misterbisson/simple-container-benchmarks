#!/bin/bash

# if we don't have a target var, then we're running in server mode
# ...so start iperf, otherwise we start the client script
if [[ ! -n "$TARGET" ]]
then
	node /server/server.js && iperf -s
else
	/usr/local/sbin/simple-container-benchmarks
fi
